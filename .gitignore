# Secrets — never commit
mac/.build-config
install/.env
**/.env
!**/.env.example

# Build outputs
mac/build/*.pkg
mac/build/*.zip
mac/build/*.cer
mac/build/*.key
mac/build/*.certSigningRequest

# macOS
.DS_Store
**/.DS_Store

# Dependencies
node_modules/
